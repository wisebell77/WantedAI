# Python 3.11+ (개발·검증 환경 3.13.9)

# --- 핵심 (추천을 돌리는 데 필요)
numpy>=2.0
sentence-transformers>=5.0      # 한국어 문장 임베딩. 모델은 jhgan/ko-sroberta-multitask
scikit-learn>=1.5               # AgglomerativeClustering (L2 군집)
torch>=2.4                      # sentence-transformers 의존. CPU 빌드로 충분하다

# --- 수집
requests>=2.32
python-dotenv>=1.0              # .env 의 API 키

# --- 문서 파싱
PyMuPDF>=1.24                   # PDF. 지연 import 라 서빙에는 없어도 된다
olefile>=0.47                   # HWP(OLE 복합문서) 컨테이너 해석.
#                                 파싱 로직은 자체 구현이지만 컨테이너를 여는 건 olefile 이다.
#                                 overlap/parse/hwp.py 가 **모듈 최상위에서** import 하므로
#                                 `import overlap` 자체가 실패한다 — 서빙에도 반드시 필요하다.

# --- 선택
# playwright>=1.45               # 민간 공고 중 SPA 렌더링이 필요한 것. 없으면 static 수집만 된다
#                                # PLAYWRIGHT_BROWSERS_PATH 로 브라우저 설치 위치를 지정할 수 있다

# 메모
#  - Anaconda 환경에서 torch 가 OpenMP 를 두 번 로드해 죽는 경우가 있다.
#    그때는 KMP_DUPLICATE_LIB_OK=TRUE 를 걸고 돌린다.
#  - 행렬(data/job_matrix.json)만 있으면 순위 계산은 numpy 없이도 된다.
#    무거운 의존성은 '자연어 문장 → 역량' 투영에만 필요하다.
