# `recommend/` — 대조 엔진

사용자 경험과 직무 공고를 맞춰 본다. **이 서비스의 출력이 나오는 곳이다.**

## 파일

| 파일 | 내용 |
|---|---|
| `matrix.py` | `JobMatrix`, `JobProfile` — NCS 소분류 × 역량 행렬 |
| `evidence.py` | `EvidenceIndex`, `Quote`, `Source`, `RelatedPosting`, `MarketSignal` — 근거 인덱스 |
| `scorer.py` | `Recommender`, `JobMatch`, `GapReport`, `Evidence` — 정방향·역방향 |

## 정방향과 역방향은 같은 지표의 양 끝이다

```
정방향  목표 직무와 겹침이 부족한 곳      → 준비할 것    Recommender.forward()
역방향  안 본 직무와 겹침이 뜻밖에 많은 곳  → 새 선택지    Recommender.reverse()
                                                      Recommender.unexpected()
```

**역방향이 데모 하이라이트다.** 정방향은 "그래 나도 알아"지만 역방향은
"그건 생각 못 했는데"다. `unexpected()` 는 사용자가 이미 보고 있는 대분류를
통째로 빼고 답한다 — 정보통신을 보던 사람에게 정보통신 옆칸을 권하는 건 '뜻밖'이 아니다.

정방향도 비교 직무를 함께 낸다. "부족 12개"만 보여 주면 많은 건지 적은 건지 알 수 없다.

## 입도가 소분류인 이유

| 단계 | 개수 | 문제 |
|---|---|---|
| 대분류 | 24 | 너무 거칠다. 정보통신 하나에 백엔드·보안·AI 가 다 들어간다 |
| **소분류** | **279** | **개발·기획·운영이 갈린다. 표본도 버틴다** |
| 세분류 | 1,114 | 표본이 3~9건이라 통계가 안 된다 |

## 직무를 행렬에 넣는 기준 두 가지

```
유효단위 >= 30     기관당 8단위 상한을 건 뒤의 수
HHI      <  0.25   허핀달 지수 — 한 기관 쏠림
```

단위 수만 보면 속는다. `소성·소결세라믹제조`는 100단위로 상위권인데
기관이 2곳(한 곳 96%)이라 유효단위 12 로 떨어진다. 통계가 아니라 사례다.

## 점수 = IDF × √(그 직무 안 요구 비율)

IDF 만 쓰면 `몇 개 직무에 나타나는가`만 본다. 그 직무 안에서 1건이 요구했든
100건이 요구했든 같은 무게가 된다.

```
자동차운전·운송   회의운영 공고   1건   ┐ 같은 무게로 더해져
일반사무         회의운영 공고 100건   ┘ 1건짜리 쪽이 이겼다
```

`데이터분석` 프리셋의 1위가 `자동차운전·운송` 이던 게 그 결과다.

**raw df 를 곱하면 안 된다.** 큰 직무일수록 df 가 크므로 표본 크기 편향이
되살아난다. 비율(df/units)을 쓴다. √ 를 씌우는 건 네 방식을 재 보고 골랐다
(`Settings.df_weight`).

부수 효과로 화면의 역량 순서도 나아진다 — `회의운영(공고 100건)` 이
`조직의연간행사일정(공고 1건)` 보다 위에 온다.

## 순위는 점수로, 거르기는 개수로 — 기준이 둘이다

`reverse_from` 은 점수로 줄을 세운 뒤 `min_overlap` 으로 거른다.
기준이 다르므로 **점수 1위가 잘려 나갈 수 있다.** 실제로 그랬다 —
`min_overlap=3` 일 때 점수 1위(정보기술개발 1.10)가 겹침 2개라 빠지고
3위가 화면에 올라갔고, 사용자 질의의 **35%가 아예 답을 못 받았다.**

```
min_overlap   사용자 Top-1   Top-3   답 못한 질의
     3           36.4%      39.8%      35%
     2           47.7%      61.4%      12%   <- 지금 값
     1           45.5%      64.2%       8%
```

## 역량을 상위 300개로 자르는 기준

`df × IDF` 순이다. 둘 중 하나만 쓰면 망가진다.

- `df` 만 → `문서작성`·`의사소통` 같은 범용어가 상위를 차지한다.
- `IDF` 만 → 딱 한 번 나온 파싱 오류가 1등이 된다(가장 희귀하므로).

곱해야 "이 직무가 실제로 요구하면서 고유한 것"이 남는다.
300과 500이 같은 성능이라 가벼운 쪽을 쓴다.

## 출력 규칙 — 어기면 서비스가 아니라 점집이 된다

1. **판정하지 않는다.** `JobMatch.sentence()` 가 화면 문구의 기준형이다.
2. **근거 없는 항목은 내보내지 않는다.** 아래 '근거' 절을 볼 것.
3. **퍼센트를 쓰지 않는다.** 내부 점수(IDF 합)는 정렬 용도이고 화면에 띄우지 않는다.

## 근거 — 이 폴더에서 가장 신경 쓴 부분

역량 하나에 **양쪽 근거**가 붙는다. 한쪽만 있으면 화면에 올리지 않는다.

```
서버보안소프트웨어설치및운영                      공고 22건에서 요구
  당신의 경험 — "사내 서버 취약점 점검해서 보안 패치 적용"
  공고 원문   — "서버 보안 및 단말 보안 소프트웨어 설치 및 운용기술"
                한국○○공단 2024년 신입 채용 [원문 보기]
```

행렬에는 역량이 **접힌 이름**으로만 들어 있다. 공고에 그렇게 적혀 있는 게 아니라
L1·L2 를 거친 결과다. 그래서 `EvidenceIndex` 가 원문 표기를 따로 되찾아 둔다.
부산물로 얻는 게 하나 더 있다 — 같은 노드에 붙은 원문 표기를 나란히 보여 주면
"글자가 다 다른데 같은 역량으로 묶였다"가 그대로 보인다. **L2 가 일한 증거다.**

**부족한 역량에도 똑같이 붙는다.** 그것도 "이 직무가 이걸 요구한다"는 주장이므로
근거가 필요하다. `require_quotes` 가 켜져 있으면 공고 원문을 못 찾은 역량은
`have` 든 `lack` 이든 버린다. 인덱스를 아직 안 만들었으면(평가·개발 중)
그 규칙이 꺼지는데, 조용히 꺼지는 게 아니라 flag 로 드러나 있다.

민간은 다르게 다룬다. 확장 노드는 민간 공고 출처라 **본문을 인용하지 않는다.**
`MarketSignal` 이 "어느 기업의 어떤 직무가 요구하는지"까지만 내고,
직무 판정과 섞지 않고 별도 블록으로 나간다.

```
[민간 축] 공공 직무기술서에는 없지만 시장이 요구하는 것
  · Spring — 민간 공고 9개 직무 · 기업 2곳이 요구하고 있습니다
             (공공 축에서는 소프트웨어개발프레임워크)
```

크기는 1.9MB(gzip 0.4MB)다. 직무 하나만 떼면 gzip 49KB 라 펼칠 때 불러와도 된다.

## 한 단계 더 좁히기 — 세분류

소분류까지만 보면 안 보이는 게 있다. `정보기술전략·계획` 안에 **빅데이터분석이
27단위** 들어 있는데, 소분류 이름만으로는 그게 데이터 직무인 줄 모른다.

그래서 세분류 행렬을 따로 만든다(`JobMatrix.build(level=8)`).
**추천 후보가 아니다** — 이미 고른 소분류 안에서 줄을 세우는 용도다.
표본이 작아 단독 근거로 못 쓰므로 문턱을 유효단위·HHI 가 아니라 단위 수로만 본다.
거기에 유효단위를 걸면 정작 보여 주고 싶은 `빅데이터분석` 이 통째로 사라진다.

```
정보기술전략·계획   공고 188건 · 겹침 7개
  └ 정보기술전략    겹침 9 · 공고 82건
  └ 빅데이터분석    겹침 8 · 공고 27건    ← 소분류만 보면 안 보인다
  └ 정보기술기획    겹침 7 · 공고 70건
```

## 분류 이름이 안 와닿는 문제

`정보기술전략·계획`, `건축설비설계·시공`, `산업안전관리공통직무` — NCS 를 모르면
이름만 보고는 무슨 일인지 알 수 없다. 분류표에는 코드와 이름뿐이라
고용24에서 **능력단위 목록**을 받아 붙인다(`collect/jobinfo.py`).

정의 문장 하나를 골라 쓰지 않는다. 능력단위 정의는 그 단위 하나를 설명하는
문장이지 분류 전체를 설명하는 문장이 아니다. 첫 정의를 쓰면
`정보기술전략·계획` 이 "핀테크 기술 분석이란…" 으로 소개된다 —
여덟 능력단위 중 하나일 뿐인데 분류 전체인 양 나간다.

```
빅데이터분석  →  빅데이터 분석 결과 평가 · 분석 데이터 피처 엔지니어링 ·
                빅데이터 분석 결과 시각화 · 빅데이터 분석 모델링 · 분석 데이터 전처리
```

이름 나열이 더 정확하고 더 잘 읽힌다. 123개 중 93개를 받았고,
못 받은 30개는 **설명 없이 나간다** — 지어내지 않는다.

## 다른 폴더와의 관계

- `competency/` 에서 사전과 투영기를 받는다. 역량 문자열을 직접 다루지 않는다.
- `taxonomy/` 에서 코드 ↔ 이름을 받는다.
- `evaluate/` 가 `JobMatrix.effective_units()` / `hhi()` 를 그대로 쓴다 —
  평가와 서비스가 같은 기준을 쓰지 않으면 평가가 의미를 잃는다.
- 행렬과 근거 인덱스는 `pipelines/build_matrix.py` 가 **같이** 만든다.
  따로 돌리면 사전이 바뀌었을 때 행렬에는 있는데 근거는 비는 역량이 생긴다.
  서비스는 그 파일만 읽으면 되고, 임베딩 모델은 **투영에만** 필요하다.
  (역량 목록을 이미 가진 호출자는 `JobMatrix.rank()` 만으로 모델 없이 돌릴 수 있다)
