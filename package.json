{
  "name": "overlap-career-coach-mvp",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server/local-server.mjs",
    "dev:recommendation": "OVERLAP_DATA=./data/overlap/산출물 python3 web/server.py --port 8000",
    "build": "esbuild src/next-step.js --bundle --minify --sourcemap --outfile=dist/next-step.js",
    "test": "node --test tests/*.test.mjs",
    "transcribe": "python server/transcribe.py",
    "analyze-video": "python server/analyze_video.py"
  },
  "dependencies": {
    "motion": "^12.23.24"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
