UPSTAGE_API_KEY=
UPSTAGE_FAST_MODEL=solar-mini
UPSTAGE_COACH_MODEL=solar-pro4
PORT=4173
OVERLAP_DATA_DIR=data/overlap
# Optional separate recommendation container/API. The frontend never falls back to mock results.
RECOMMENDATION_BASE_URL=http://127.0.0.1:8000
# Optional: override the directory or individual MediaPipe task model paths.
MEDIAPIPE_MODEL_DIR=models
MEDIAPIPE_FPS=10
